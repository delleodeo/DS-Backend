// const { trackProductView, trackVendorView, getVisitorId } = require("./subscriptor.sevice.js");

// exports.productView = async (req, res) => {
//   const visitorId = getVisitorId(req);
//   const result = await trackProductView({ productId: req.params.productId, visitorId });
//   res.json(result);
// };

// exports.vendorView = async (req, res) => {
//   const visitorId = getVisitorId(req);
//   const result = await trackVendorView({ vendorUserId: req.params.vendorId, visitorId });
//   res.json(result);
// };
